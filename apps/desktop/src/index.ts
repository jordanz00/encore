/**
 * Desktop renderer entry. Today the Tauri shell loads
 * http://localhost:3000 (the Next.js web app) directly via
 * tauri.conf.json devUrl. This file is reserved for native-bridge
 * code (system media keys, mini-player window control, tray menu).
 */
export {};
